apiVersion: v1
kind: Service
metadata:
  name: {{ include "socket-server-develop.fullname" . }}
  labels:
    {{- include "socket-server-develop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "socket-server-develop.selectorLabels" . | nindent 4 }}
