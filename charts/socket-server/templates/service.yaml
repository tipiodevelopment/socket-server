apiVersion: v1
kind: Service
metadata:
  name: {{ include "socket-server.fullname" . }}
  labels:
    {{- include "socket-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "socket-server.selectorLabels" . | nindent 4 }}
