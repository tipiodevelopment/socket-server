apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "socket-server.fullname" . }}-test-connection"
  labels:
    {{- include "socket-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "socket-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
