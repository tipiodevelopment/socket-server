node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.local
.env.test
# Local backups taken before repointing DATABASE_URL — same secrets as .env.
.env.backup*
.env.*.backup
.mcp.json
.claude/
CLAUDE.md
.codex/

# Firebase service account (SECRET — never commit)
serviceAccount.json
*firebase-adminsdk*.json
.secrets/
